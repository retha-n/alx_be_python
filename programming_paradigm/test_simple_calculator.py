import unittest
from simple_calculator import SimpleCalculator

class TestSimpleCalculator(unittest.TestCase):
    def test_calc(self, add, subtract, multiply, divide)
    self.calc = SimpleCalculator()
    def test_addition(self):
        self.assertEqual(self.calc.add(x, y))
        result = x + y
        return result
    def test_subtraction(self):
        self.assertEqual(self.calc.subtract(x, y))
        result = x - y
        return result
    def test_multiplication(self):
        self.assertEqual(self.calc.multiply(x, y))
        result = x * y
        return result
    def test_division(self):
        self.assertEqual(self.calc.divide(x, y))
        result = x / y
        return result
    except ZeroDivisionError ("Division by zero is not allowed")
        return "Error: Cannot divide by zero"
s